import {Component} from '@angular/core';

@Component({
  selector: 'app-not-available-module',
  template: '<app-not-available-page></app-not-available-page>'
})

export class NotAvailableModuleComponent {
  constructor(
  ) {
  }
}
